export function Toaster() {
  // Placeholder: your project imports Toaster from this path in some templates.
  // The rest of the app uses `sonner` directly; keeping this prevents import errors.
  return null;
}

